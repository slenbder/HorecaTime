# HorecaTime — Контекст проекта

## Суть проекта
Python/aiogram 3 Telegram-бот для учёта рабочего времени в ресторане.  
Мигрируем с Google Apps Script (файлы скриптов доступны в ассетах проекта — референс логики).  
Репозиторий: https://github.com/slenbder/HorecaTime

---

## Текущий фокус

**Post-Stage 16 фиксы завершены ✅ (2026-05-13-14)** — 6 критичных багов исправлены (см. HISTORY.md)

**Stage 16 завершён ✅** — Система карт лояльности и наполняемости чеков
- Разделён FSM официанта: смена → карты → чеки (три этапа)
- Фантомный сотрудник "Наполняемость чека" (ID: 1984002026)
- Два типа апрува: карты лояльности → AH офику, чеки → фантому с суммированием
- Отображение общего пула чеков в /hours_* для Официанта
- Перенос фантома при switch_month()
- 122 тестов (77 → 112 → 122)

**Активная ветка:** main (после мерджа feature/loyalty-and-checks)
**Тесты:** 122/122 passing
**Production:** VPS 5.129.215.239:/opt/horecatime

**fix/manual-user-recovery завершён ✅ (2026-05-03):**
- Добавлена команда `/restore_user` для ручного восстановления юзера в месячном листе
- Новый FSM state `waiting_restore_user` в `AuthStates`
- Логирование с префиксом `MANUAL_RESTORE`

**Этап 14 завершён:**
- Фикс формул S/AJ/AK в google_sheets.py (английские → русские СУММПРОИЗВ)
- ADMIN_*_IDS удалены из .env → динамическое чтение из SQLite
- Уведомления админам работают автоматически при promote/demote
- Production: git clone на 5.129.215.239, Docker DNS 8.8.8.8

**Stage 15 завершён ✅ — Рефакторинг системы ставок:**
- Удалена таблица `rates` (шаблоны ставок)
- Все ставки теперь персональные через `user_rates`
- `/set_rate` — установка ставки конкретному сотруднику
- Удалена команда `/set_rate_all` (редактирование шаблонов больше не нужно)
- Добавлена таблица `user_rates_future` — планирование изменений ставок
- При увольнении (`delete_user`) теперь удаляются записи из `user_rates`, `user_rates_future`, `user_rates_history`
- 73 теста (было 55)

**Stage 15+ завершён ✅ — Критичные фиксы (2026-05-01):**
- LEFT JOIN фикс задеплоен (коммит d6a7ce2) — новые сотрудники без ставок теперь видны в `/set_rate`
- Список команд обновлён (коммит 3c52d9a) — `/set_rate_all` удалена из superadmin, `/set_rate` добавлена, команды упорядочены по приоритету
- Фикс `delete_user()` (коммит d1d15c2) — ставки удаляются при увольнении сотрудника

---

## Известные баги

- ⚠️ Двойное нажатие "Одобрить" → ошибка "message is not modified"
- ⚠️ `/hours_first` показывает зарплату даже если ставка не установлена (нужна проверка)

---

## Стек

- Python 3.11+, aiogram 3.13.1
- gspread 6.1.2, oauth2client 4.1.3
- APScheduler 3.10.4, aiosqlite 0.20.0
- **SQLite** — FSM + кеш ролей + персональные ставки (НЕ основная БД данных сотрудников)
- **Google Sheets** — единственный источник данных сотрудников и смен (single source of truth)
- **Docker** — multi-stage Dockerfile + docker-compose.yml, SQLite в volume `data/`

---

## Критичные технические решения

### Терминология (важно!)
- **Отдел** (department) = Зал/Бар/Кухня/МОП — подразделение в ресторане
- **Позиция** (position) = Официант/Раннер/Бармен — роль сотрудника в отделе
- **Должность** (custom_position) = "Шеф ЗЦ", "Бренд-шеф" — только для Руководящий состав и Грузчик/Закупщик
- Техлист: **E = базовая позиция** (всегда каноническая), **H = custom_position** (пусто для остальных)

### БД и архитектура
- **Google Sheets** = source of truth для всех данных сотрудников и смен
- **SQLite** = только FSM состояния + кеш ролей + персональные ставки (`user_rates`)
- Название месячного листа: `"{Месяц} {Год}"` (например "Апрель 2026")
- Техлист: A(TG_ID), B(@Ник), C(ФИО), D(Отдел), E(Позиция), F(Дата рег), G("ДА"), **H(Должность)**
- Месячный лист: **B (TG_ID) и D:AK** = "Обычный текст" формат обязателен (B — иначе научная нотация ломает switch_month; D:AK — иначе числа интерпретируются как даты)
- При ручном создании строк (фантом и др.) — выставлять Plain text для B и D:AK вручную
- `value_input_option="RAW"` для пользовательских данных (защита от formula injection)
- `value_input_option="USER_ENTERED"` только для hardcoded формул (S/AJ/AK/AL)

### Роли и права
- **SUPERADMIN_IDS / DEVELOPER_ID** импортируются из `config.py`
- Суперадмины и developer **не регистрируются** в таблице `users`
- Проверка прав — всегда через константы из config.py (НЕ из SQLite)
- admin_hall управляет отделами: **Зал + МОП**

### Фантомный сотрудник (наполняемость чеков)
- **TG_ID:** 1984002026 (хардкод в config.py)
- **Не регистрируется** через approve flow (только строка в таблице)
- **Позиция в листе:** первая строка в блоке Официантов
- **Запись данных:** суммирование в пределах дня (3 чека + 2 чека = 5 в ячейке)
- **Формулы:** простые SUM (без разделения H/AH)
- **Ставка:** 1500 р/час (хардкод, для будущих расчётов)
- **Переносится** при switch_month() с обнулением данных

### Ставки и зарплата
- **Персональные ставки** в `user_rates` (SQLite) — у каждого сотрудника своя, `rates` удалена
- При апруве ставка **НЕ устанавливается** автоматически — администратор ставит вручную через `/set_rate`
- `user_rates_future` — планирование изменений ставок "со следующего месяца"
- При `switch_month()` снимки: `user_rates_history`; `user_rates_future` применяется через `apply_future_rates()`
- Расчёт зарплаты ТОЛЬКО через `user_rates`

### Позиции с повышенной ставкой
- **Раннер:** базовая (будни) + повышенная (пт/сб/вс)
- **Бармен/Барбэк:** базовая (до 60ч) + повышенная (>60ч + AH)
- Ввод ставки через FSM: сначала базовая, потом повышенная (2 отдельных шага)

### Увольнение
- Ячейка A в месячном листе → `#FFCCCC`
- Удаление из Техлиста
- Строка **НЕ удаляется** из месячного листа (история)
- Красные строки **НЕ переносятся** в новый месяц
- Удаление из SQLite (`users`, `user_rates`, `user_rates_future`, `user_rates_history`) + сброс FSM/кеш/команд

### Telegram ограничения
- **callback_data** максимум 64 байта — данные в `_pending_admins`, не в callback
- Email администратора — только `@gmail.com`
- `LinkPreviewOptions(is_disabled=True)` во всех HTML-сообщениях с упоминаниями
- `html.escape()` для всех user inputs в HTML-сообщениях

### Approve flow (после рефакторинга PR #56 + Phase 4)
- **Нет reverse mapping** — `position` и `custom_position` читаются напрямую из E и H Техлиста
- **Один линейный путь** для всех позиций (нет Path A/B, нет FSM-ветки для admin)
- `process_approve` = оркестратор из 5 приватных функций

---

## Workflow — трёхслойная система

```
Claude (claude.ai)        →  Планирование, архитектура, промпты
        ↓
Claude Code (terminal)    →  Выполнение кода
        ↓
Отчёт → Claude           →  Анализ результатов
```

### Формат промптов для Claude Code (русский язык!)
```
Ветка: refactor/phase-4-monoliths
Файл: app/bot/handlers/auth.py
Проблема: [описание]
Фикс: [шаги решения]
Требования: [проверки, pytest 56/56]
Commit: "refactor(scope): описание"
```

### Правила работы
- **Один промпт = одна задача** — последовательное выполнение
- Тесты (`pytest tests/` — 56 passing) после каждого изменения
- Прямые коммиты в feature-ветку (без PR до стабилизации)
- Решения формализуются в документацию **в реальном времени**
- Token efficiency: новая вкладка per phase, минимум файлов в контексте

### Обработка вопросов Claude Code
- Структурные/технические → Slenbder отвечает напрямую
- Бизнес-логика/неоднозначности → консультация с Claude (claude.ai)
- Ответы ищем в `docs/archive/FINAL_AUDIT.md` (раздел "Предложение" для каждого бага)

---

## Ключевые паттерны и принципы

### Логирование
- **НИКОГДА** `print()` — только `logging`
- Три лог-файла: `app.log` (общий), `errors.log` (ошибки), `googleapi.log` (Sheets API)
- Email маскируется: `p***r@gmail.com` через `mask_email()` из `app/utils/text_utils.py`
- **НЕ логировать PII**: `full_name` не пишем в логи (только `telegram_id`)
- Логировать: старт/стоп, API calls, реконнекты, approve/reject

### Уведомления
- Админы читаются динамически из SQLite через `get_admins_by_department()`
- Зал/МОП → admin_hall + SUPERADMIN_IDS
- Бар → admin_bar + SUPERADMIN_IDS
- Кухня → admin_kitchen + SUPERADMIN_IDS
- При `/promote` юзера → сразу начинает получать уведомления
- При `/demote` админа → сразу перестаёт получать уведомления

### Регистрация callback handlers
- Специфичные раньше общих: `approve_ah_callback` ДО `approve_`/`reject_`
- Порядок регистрации роутеров: auth → userhours → userreports → admin → superadmin

### Константы и маппинг
- Все списки позиций, отделов → `config.py`
- `POSITIONS_WITH_EXTRA = {"Раннер", "Бармен", "Барбэк"}`
- Маппинги отделов, позиций в `docs/TECH_REFERENCE.md`

### Google Sheets формат
- **B (TG_ID)** = "Обычный текст" обязателен — без этого Sheets показывает научную нотацию, switch_month не находит сотрудника
- D:AK = "Обычный текст" (настраивается вручную или через ensure_user_in_current_month_hours)
- Формулы S/AJ/AK вставляются автоматически (`USER_ENTERED`)
- AL/AM/AN = числовой формат (выходные часы Раннера)

---

## Важные файлы проекта

- **docs/archive/FINAL_AUDIT.md** — архив аудита Phases 1-4 (завершён), остаётся локально
- Контекстные файлы CLAUDE.md, HISTORY.md, TECH_REFERENCE.md хранятся в отдельном приватном репо: https://github.com/slenbder/HorecaTime-documentations
- **Инструкции/** — пользовательские инструкции по ролям (Зал/Бар/МОП/Админы/Суперадмин). Обновлять синхронно с изменением механик — наравне с TECH_REFERENCE.md
- **config.py** — константы, ID, списки позиций
- **app/utils/text_utils.py** — `make_mention()`, `mask_email()`
- **app/db/models.py** — в .gitignore! Требует миграцию на деплое
- **app/utils/formatting.py** — fmt_hours(), fmt_money(), fmt_emp_rate()
- **migrate_user_rates_once.py** — одноразовая миграция (оставлен как документация)

---

## На горизонте

### Ближайшие задачи
- **Monitoring** — алерты при ошибках записи в таблицу
- **Фидбэк клиента** — возможные доработки после сдачи

### Долгосрочные планы
- Лицензионная система (license server + PyArmor)
- Универсализация для сторонних клиентов
- Web-панель для конфигурации

---

## Key Learnings

- **Отдел ≠ Позиция ≠ Должность** — путаница в терминах ведёт к багам
- **Техлист E = базовая позиция, H = custom_position** — E всегда каноническая, H только для РС/Грузчик/Закупщик
- **Нет reverse mapping** — position и custom_position читаются напрямую из Техлиста E и H
- **Approve — один линейный путь** — process_approve = оркестратор из 5 приватных функций
- **callback_data < 64 байт** — данные в dict, не в callback
- **D:AK "Plain text"** — предотвращает интерпретацию дат
- **value_input_option="RAW"** — для user data; "USER_ENTERED" только для формул
- **html.escape()** — защита от HTML injection
- **user_rates для расчёта** — единственный источник ставок, `rates` удалена
- **Token efficiency** — новая вкладка per phase
- **LEFT JOIN в get_users_rates_by_department обязателен** — INNER JOIN исключает новых сотрудников без ставок → замкнутый круг (нужна ставка чтобы назначить ставку)
- **app/db/models.py в .gitignore** — миграция на деплое; при изменении требует `git add -f app/db/models.py`
- **Решения сразу в docs**, не откладывать
- **admin_* могут вносить смены**: проверка роли в `/shift` включает `admin_hall/bar/kitchen`
- **SQLite WAL mode + timeout** — предотвращает "database is locked" при конкурентных запросах
- **PII в логах**: full_name не логируем, только telegram_id
- **admins видны при увольнении**: фильтр `role == 'user'` в dismiss убран
- **ADMIN_*_IDS удалены** — уведомления через `get_admins_by_department()` из SQLite
- **Git на production** — /opt/horecatime теперь git repo, деплой через git pull
- **Ставки ТОЛЬКО персональные** — нет больше шаблонов `rates`, каждый сотрудник имеет запись в `user_rates`
- **delete_user() чистит всё** — при увольнении удаляются записи из users + user_rates + user_rates_future + user_rates_history
- **Production деплой через deploy.sh** — скрипт делает git pull на сервере + rebuild + restart
- **Telegram API может блокироваться ISP** — решение через /etc/hosts override на альтернативный IP
- **Колонка B месячного листа — Plain text обязателен** — Автоматический формат даёт научную нотацию, switch_month не матчит TG_ID → удаляет строку
- **Числа в Sheets пишутся как int/float, не str** — str("2") не суммируется СУММ()
- **float() всегда через .replace(",",".")** — русская локаль Sheets даёт "1,5"
- **update_cell не принимает value_input_option** — использовать ws.update()
- **Утилиты форматирования** — только в app/utils/formatting.py,
  не дублировать в handlers
- **Константы колонок Sheets** — COL_S/AJ/AK/AL/AM/AN в config.py,
  не хардкодить числа в коде
- **switch_month читает Техлист один раз** — `get_techlist_ids()` до цикла переноса, проверка `str(tg_id).strip() in techlist_ids` в памяти; `user_exists_in_techlist()` в цикле = N+1 → 429 при N≈55

---

## Контактные данные

- **Developer Telegram ID:** `8624217185` (в .env как `DEVELOPER_ID`)
- **Test account Telegram ID:** `6073294261`
- **Client contact:** Ариф
- **Test restaurant:** Собственный ресторан Slenbder
